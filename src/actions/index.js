import ActionTypes from './actiontypes';

import API from '../api';

function fetchFavorableLists () {
    return (dispatch) => {
        
    }
}
const MainActions = {
    fetchFavorableLists
};
export {
    ActionTypes,
    MainActions
};
