import React, {Component} from 'react';
import {
    ListView
} from 'react-native';

class FListView extends Component {
    formatData(data){
        const dataBlob = {};
        const sectionIds = [];
        const rowIds = [];
    }
    constructor(props) {
        super(props);
        const getSectionData = (dataBlob, sectionId) => dataBlob[sectionId];
        const getRowData = (dataBlob, sectionId, rowId) => dataBlob[]
    }

}


